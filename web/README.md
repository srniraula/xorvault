# XORFS Web UI (development)

This is a minimal React UI for the XORFS service.

Requirements:
- Node.js (>=18 recommended)

Install & run (development):

```bash
cd web
npm install
npm run dev
```

The dev server runs on http://localhost:5173 and talks to the API on http://localhost:8080 (CORS enabled in dev).
